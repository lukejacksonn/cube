{
  "name": "rubiks-cuber",
  "type": "module",
  "version": "0.0.3",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "npm run build && git add . && git commit -m 'deploy' && git push origin master"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.8.1",
    "puppeteer": "^21.7.0",
    "vite": "^5.0.12"
  },
  "dependencies": {
    "goober": "^2.1.14",
    "preact": "^10.19.3"
  }
}
